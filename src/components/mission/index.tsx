import React, {useState} from 'react';import config from '../../config'import {useQuery} from "../../hooks/useQuery";const Mission = () => {    document.title = 'Agrarian | About'    const [active, setActive] = useState('1');    const isMobile = useQuery();    const [data, setData] = useState(config.data.mission);    console.log(config.bgImages.mission)    const setHandler = (data: any, active: any) => {        setActive(active);        setData(data)    }    return (        <>            <div className="container my-4">                <div className="row">                    <div className="col-md-2 container ab-left">                        <div className="row">                            <div                                className={`${isMobile ? 'col-4' : 'col-md-12 mt-3'}  mb-2 text-center l-btn d-flex justify-content-center align-items-center`}                                onClick={() => setHandler(config.data.mission, '1')}>                                <h4 className={active === '1' ? 'active' : ''}>MISSION</h4>                            </div>                            <div                                className={`${isMobile ? 'col-4' : 'col-md-12'} mb-2 text-center l-btn d-flex justify-content-center align-items-center`}                                onClick={() => setHandler(config.data.aim, '2')}>                                <h4 className={active === '2' ? 'active' : ''}>AIM</h4>                            </div>                            <div                                className={`${isMobile ? 'col-3' : 'col-md-12'} mb-2 text-center l-btn d-flex justify-content-center align-items-center`}                                onClick={() => setHandler(config.data.focus, '3')}>                                <h4 className={active === '3' ? 'active' : ''}>FOCUS</h4>                            </div>                        </div>                    </div>                    <div className="col-10">                        {/*<img src={config.bgImages.mission} alt='mission' />*/}                        {/*<div className=" h-100 w-100" style={styles}>*/}                        <div className="text-wrap ">                            <ol>                                {                                    data.map((ele, idx) => {                                            return <li className='text-wrap mb-4 mx-0 ab-data' key={idx}>{ele}</li>                                        }                                    )                                }                            </ol>                        </div>                        {/*</div>*/}                    </div>                </div>            </div>        </>    )}export default Mission;const styles = {    background: `url(${config.bgImages.mission}`,    backgroundRepeat: 'no-repeat',    backgroundPosition: 'center',}