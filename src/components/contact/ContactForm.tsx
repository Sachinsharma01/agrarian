import React, {useState} from 'react';import sendToFirebase from "../../utils/sendToFirebase";export const ContactForm = () => {    const [firstName, setFirstName] = useState('')    const [email, setEmail] = useState('')    const [phone, setPhone] = useState('')    const [message, setMessage] = useState('')    const [lastName, setLastName] = useState('')    const submitHandler = (e: any) => {        e.preventDefault();        const data = {            Name: firstName,            Email: email,            Subject: phone,            Message: message,        }        const res = sendToFirebase(data);        console.log(res)        setFirstName('')        setLastName('')        setMessage('')        setPhone('')        setEmail('')    }    return (        <>            <form className="container-fluid ">                <div className="row my-4 ">                    <div className="container col-md-10">                        <div className="container contact-form">                            <div className="row mt-4 ">                                <h2 className='fs-2 mt-2 text-center'> Get In Touch With Us</h2>                                <p className='text-center'>Feel free to use the contact form below to contact us                                    directly.</p>                                <div                                    className='col-12 d-flex justify-content-between align-items-center gap-3 flex-wrap'>                                    <input name='firstName' value={firstName} type='text'                                           onChange={(e) => setFirstName(e.target.value)}                                           className='col-md-5 col-12 rounded-3 p-2'                                           placeholder='Enter First Name'/>                                    <input value={lastName} type='text' onChange={(e) => setLastName(e.target.value)}                                           className='col-md-5 col-12 rounded-3 p-2'                                           placeholder='Enter Last Name'/>                                </div>                                <div                                    className='col-12 mt-4 d-flex justify-content-between align-items-center gap-3 flex-wrap'>                                    <input value={phone} type='number' onChange={(e) => setPhone(e.target.value)}                                           className='col-md-5 col-12 rounded-3 p-2'                                           placeholder='Enter Your Number'/>                                    <input value={email} type='email' onChange={(e) => setEmail(e.target.value)}                                           className='col-md-5 col-12 rounded-3 p-2'                                           placeholder='Enter Your Email'/>                                </div>                                <div className='col-12 my-4'>                                    <textarea value={message} onChange={(e) => setMessage(e.target.value)}                                              className='w-100 rounded-3 p-1'                                              placeholder='Message....'></textarea>                                </div>                                <div className="container d-flex justify-content-center align-items-center w-100">                                    <input type='submit' onClick={submitHandler}                                           className='btn col-md-2 col-10 mb-2 btn-primary'/>                                </div>                            </div>                        </div>                    </div>                </div>            </form>        </>    )}